/**
 * Confidence below this routes a change to the human Review Queue instead of
 * the main Intel Feed. AI is decision-support, never the final authority.
 */
export const REVIEW_CONFIDENCE_THRESHOLD = 0.6;
